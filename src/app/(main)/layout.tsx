// This file can be removed or left empty as its logic is now handled in the root layout and client-layout.
// For now, we will keep it simple.
export default function MainLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
